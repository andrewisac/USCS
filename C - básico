#include <stdio.h> 

int main()
{ 
    // Variáveis 
   
        int dias=0, idadeAnos, dia, mes, ano;
    
    // Entrada 
   
        printf("Digite a sua idade: "); 
        scanf("%d", &idadeAnos);
    
    // Processamento 
    
        while (idadeAnos < 0){
            
             printf("\nA idade não pode ser negativa");
             
             printf("\n\nDigite a sua idade: "); 
             scanf("%d", &idadeAnos);
    
        }
    //**********************************************************
        //  if (idadeAnos < 0) {
                //printf("idade não pode ser negativa");
        //  }
    //**********************************************************
        
            dias = (idadeAnos * 365);     
        // Saída
            printf ("A quantidade de dias é: %d", dias);
   
    return 0; 
}

